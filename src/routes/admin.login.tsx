import { createFileRoute, useNavigate } from "@tanstack/react-router";
import { useState } from "react";
import { login } from "@/lib/admin-data";

export const Route = createFileRoute("/admin/login")({ component: Login });
function Login() { const navigate=useNavigate(); const [email,setEmail]=useState(""); const [password,setPassword]=useState(""); const [error,setError]=useState(""); const submit=(e:React.FormEvent)=>{e.preventDefault(); if(email.trim()&&password.trim()){login();navigate({to:"/admin/dashboard"})}else setError("Renseignez votre e-mail et votre mot de passe.");}; return <main className="grid min-h-screen place-items-center px-4"><form onSubmit={submit} className="w-full max-w-md rounded-[2rem] glass gold-border p-8 shadow-2xl shadow-black/30"><div className="text-xs uppercase tracking-[.25em] text-gold">Espace sécurisé</div><h1 className="mt-3 text-4xl font-bold">Rouling Car<br/><span className="text-gradient-red">Administration</span></h1><p className="mt-3 text-sm text-foreground/65">Connectez-vous pour gérer votre flotte et vos réservations.</p><label className="mt-7 block text-sm text-foreground/70">E-mail<input value={email} onChange={e=>setEmail(e.target.value)} type="email" className="input-base mt-1" placeholder="admin@roulingcar.ma"/></label><label className="mt-4 block text-sm text-foreground/70">Mot de passe<input value={password} onChange={e=>setPassword(e.target.value)} type="password" className="input-base mt-1" placeholder="••••••••"/></label>{error&&<p className="mt-3 text-sm text-red-300">{error}</p>}<button className="mt-6 w-full rounded-full bg-primary px-5 py-3.5 font-semibold text-white shadow-lg shadow-primary/30">Se connecter</button><p className="mt-5 text-center text-xs text-foreground/45">Mode local : tout e-mail et mot de passe non vides permettent la démonstration.</p></form></main> }
